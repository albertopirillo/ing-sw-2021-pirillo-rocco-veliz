package it.polimi.ingsw.model;

/**
 * Enum used by the player to specify that it wants to use one or more leader abilities
 */
public enum AbilityChoice {
    STANDARD,
    FIRST,
    SECOND,
    BOTH
}
